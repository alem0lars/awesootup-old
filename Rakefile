require 'nanoc3/tasks'

task :watch do
  sh 'bundle exec nanoc watch'
end

task :compile do
  sh 'bundle exec nanoc compile'
end

task :view do
  sh 'bundle exec nanoc view'
end

task :irb do
  sh 'bundle exec irb'
end
